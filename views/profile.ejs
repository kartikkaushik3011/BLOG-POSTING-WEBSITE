<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="./stylesheets/style.css">
    <title>Document</title>
</head>

<body>
    <div class="w-full min-h-screen bg-fixed bg-zinc-900 text-white p-10">
        <a href="/logout" class="bg-red-500 px-3 py-2 rounded-md inline-block mb-5">Logout</a>
        <div class="flex item-start gap-3">
            <div class="w-10 h-10  rounded-md overflow-hidden">
                <a href="/profile/upload"><img class="w-full h-full object-cover" src="/images/uploads/<%=user.profilepic%>" alt="..."></a>
            </div>
            <h1 class="text-3xl mb-2">Hello!! <%= user.name%>
            </h1>
        </div>
        <form action="/post" method="post">
            <h4 class="mb-2 mt-3">Create a new post</h4>
            <textarea name="content"
                class="w-1/3 mt-5 resize-none px-3 py-2 bg-transparent outline-none border-zinc-700 border-2 rounded-md block"
                placeholder="What's on your mind?"></textarea>
            <input class="px-5 py-2 bg-blue-500 rounded-md submit mt-3" type="submit" value="Create Post">
        </form>
        <div class="posts mt-20">
            <div class="text-zinc-400">Your posts</div>
            <div class="post-container mt-4">
                <%user.posts.reverse().forEach((post)=>{%>
                    <div class="post w-1/3 p-4 rounded-md border-[1px] mb-2 border-zinc-700">
                        <h4 class="text-blue-500 mb-2">@<%=user.username%>
                        </h4>
                        <p class="text-sm tracking-tight">
                            <%=post.content%>
                        </p>
                        <div class="btns flex gap-4 mt-5">
                            <a href="/like/<%=post._id%>"
                                class="text-blue-500"><%=post.likes.indexOf(user._id)===-1?"Like":"Unlike"%></a>
                            <a href="/edit/<%=post._id%>" class="text-zinc-400">Edit</a>
                            <a href="/delete/<%=post._id%>" class="text-red-400">Delete</a>
                        </div>
                        <small>
                            <%=post.likes.length%> likes
                        </small>
                    </div>
                    <%})%>
            </div>
        </div>
    </div>
</body>

</html>